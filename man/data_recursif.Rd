% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recursif.R
\name{data_recursif}
\alias{data_recursif}
\title{Données pour l'estimation récursive d'un MCE}
\usage{
data_recursif(
  data,
  model,
  start,
  end,
  date_debut_fixe = TRUE,
  plot_var_ct = FALSE,
  dummies = NULL
)
}
\arguments{
\item{data}{data.frame contenant les données}

\item{model}{objet \code{formula} du MCE}

\item{start}{première date de la séquence récursive (chaîne "AAAA-MM-JJ")}

\item{end}{dernière date de la séquence récursive}

\item{date_debut_fixe}{si \code{TRUE} (défaut), la date de début est fixe et
la date de fin varie ; si \code{FALSE}, la date de fin est fixe et la date
de début varie}

\item{plot_var_ct}{si \code{TRUE}, toutes les variables (hors dummies) sont
retournées ; si \code{FALSE} (défaut), seulement la force de rappel et les
variables de long terme}

\item{dummies}{vecteur de noms d'indicatrices à gérer dynamiquement (ex.
\code{c("i2020q1", "i2020q2")}) ; \code{NULL} par défaut}
}
\value{
liste avec \code{df.param} et \code{df.tstat} (data.frames datés)
}
\description{
Pour chaque date de la séquence \code{[start, end]}, estime le modèle sur
la sous-période correspondante (en faisant varier la date de fin si
\code{date_debut_fixe = TRUE}, la date de début sinon) et stocke les
coefficients et t-statistiques. Les coefficients de long terme sont divisés
par la force de rappel (élasticité de long terme). Les estimations sur moins
de 40 trimestres sont ignorées.
}
